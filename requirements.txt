# Runtime + test deps for what the app actually imports. Pinned to the exact
# versions the app runs on locally (app boots + 40 tests pass on these), so the
# set is guaranteed to resolve. Future-phase packages (celery/redis/alembic/
# psycopg, mail-parser/extract-msg/imap-tools, checkdmarc/networkx/tldextract/
# structlog) were removed — unused, and their old pins made pip unresolvable.

# --- web core ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
PyYAML==6.0.3

# --- persistence (SQLite via SQLAlchemy in fixture mode) ---
sqlalchemy==2.0.52

# --- M2/M3 header + auth forensics ---
dnspython==2.8.0
dkimpy==1.1.8
pyspf==2.0.14

# --- M4 content intel (Gemini) ---
google-genai==2.19.0
python-dotenv==1.2.3

# --- M5 network intel ---
geoip2==5.3.0

# --- M6 domain intel ---
whoisit==4.0.4
rapidfuzz==3.14.5

# --- M5/insights reputation lookups ---
requests==2.34.2

# --- dev / CI ---
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.16.4
