# Runtime + test deps for what the app actually imports. Packages for later
# phases (celery/redis/alembic/psycopg, mail-parser/extract-msg/imap-tools,
# checkdmarc/networkx/tldextract/structlog) were removed — they weren't imported
# and their pins made `pip install` unresolvable on a clean Python 3.12.

# --- web core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
PyYAML==6.0.2

# --- persistence (SQLite via SQLAlchemy in fixture mode) ---
sqlalchemy==2.0.36

# --- M2/M3 header + auth forensics ---
dnspython==2.7.0
dkimpy==1.1.8
pyspf==2.0.14

# --- M4 content intel (Gemini) ---
google-genai==2.19.0
python-dotenv==1.2.3

# --- M5 network intel ---
geoip2==4.8.1

# --- M6 domain intel ---
whoisit==4.0.4
rapidfuzz==3.10.1

# --- M5/insights reputation lookups ---
requests==2.34.2

# --- dev / CI ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.4
